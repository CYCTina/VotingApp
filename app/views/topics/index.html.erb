<h1>What is your favorite technology stack?</h1>

<table class="topicTable">
  <thead>
    <tr>
      <th>Title</th>
      <th>Votes</th>
      <th>Up</th>
      <th>Delete</th>
    </tr>
  </thead>

  <tbody>
    <% @topics.each do |topic| %>
      <tr>
        <td class="tableLongTxt"><%= link_to topic.title, topic %></td>
        <td class="tableShortTxt"><%= pluralize(topic.votes.count, "vote") %></td>
        <td class="tableBtn"><%= button_to '+1',votes_path(topic_id: topic.id), method: :post %></td>
        <td class="tableBtn"><%= link_to 'Delete', topic, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '+ New Topic', new_topic_path %>
